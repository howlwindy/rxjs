<script setup lang="ts">
// import { ajax, bindCallback, bindNodeCallback, defer, from, fromEvent, fromEventPattern, generate, interval, of, range, throwError, timer, iif } from 'rxjs'
// import { combineLatest, concat, forkJoin, merge, partition, race, zip } from 'rxjs'
// import { buffer, bufferCount, bufferTime, bufferToggle, bufferWhen, concatMap, concatMapTo, exhaustMap, expand, exhaustMap, expand, groupBy, map, mapTo, mergeMap, mergeMapTo, mergeScan, pairwise, scan, switchScan, switchMap, switchMapTo, window, windowCount, windowTime, windowToggle, windowWhen } from 'rxjs/operators'
// import { audit, auditTime, debounce, debounceTime, distinct, distinctUntilChanged, distinctUntilKeyChanged, elementAt, filter, first, ignoreElements, last, sample, sampleTime, single, skip, skipLast, skipUntil, skipWhile, take, takeLast, takeUntil, takeWhile, throttle, throttleTime } from 'rxjs/operators'
// import { combineLatestAll, concatAll, exhaustAll, mergeAll, switchAll, startWith, withLatestFrom } from 'rxjs/operators'
// import { share } from 'rxjs/operators'
// import { catchError, retry, retryWhen } from 'rxjs/operators'
// import { tap, delay, delayWhen, dematerialize, materialize, observeOn, subscribeOn, timeInterval, timestamp, timeout, toArray } from 'rxjs/operators'
// import { defaultIfEmpty, every, find, findIndex, isEmpty } from 'rxjs/operators'
// import { count, max, min, reduce } from 'rxjs/operators'

// import { ref, watch } from 'vue'

// const value = ref<string>('')
// const result = ref<any>(value.value)
// const subject = new Subject<string>()
// subject.pipe(find((v) => v.includes('a'))).subscribe({
//   next: (v) => {
//     console.log(v)
//     result.value = v
//   }
// })
// watch(value, (v) => {
//   subject.next(v)
// })
</script>
<template>
  <h4><code>custom</code></h4>
  <!-- <input v-model="value" type="text" />
  <p>{{ result }}</p> -->
</template>
<style scoped></style>
